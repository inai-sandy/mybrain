# ScrapeCreators — every endpoint (generated from openapi.json, 2026-08-17)

178 endpoints, 29 platforms. **Generated — do not hand-edit.** Regenerate from `https://docs.scrapecreators.com/openapi.json`.


### tiktok — 29
- `GET /v1/tiktok/ad-library/ad` — Ad Library Ad
- `GET /v1/tiktok/ad-library/search` — Ad Library Search
- `GET /v1/tiktok/collection/videos` — Collection Videos
- `GET /v1/tiktok/creators/popular` — Get popular creators
- `GET /v1/tiktok/get-trending-feed` — Trending Feed
- `GET /v1/tiktok/live` — Live Info
- `GET /v1/tiktok/product` — Product Details
- `GET /v1/tiktok/profile` — Profile
- `GET /v1/tiktok/profile/region` — Profile Region
- `GET /v1/tiktok/search/hashtag` — Search by Hashtag
- `GET /v1/tiktok/search/keyword` — Search by Keyword
- `GET /v1/tiktok/search/suggestions` — Search Suggestions
- `GET /v1/tiktok/search/top` — Top Search
- `GET /v1/tiktok/search/users` — Search Users
- `GET /v1/tiktok/shop/product/reviews` — Product Reviews
- `GET /v1/tiktok/shop/products` — Shop Products
- `GET /v1/tiktok/shop/search` — Shop Search
- `GET /v1/tiktok/song` — Get Song Details
- `GET /v1/tiktok/song/videos` — TikToks using Song
- `GET /v1/tiktok/user/audience` — User's Audience Demographics
- `GET /v1/tiktok/user/followers` — Followers
- `GET /v1/tiktok/user/following` — Following
- `GET /v1/tiktok/user/live` — Live
- `GET /v1/tiktok/user/showcase` — User Showcase
- `GET /v1/tiktok/video/comment/replies` — Comment Replies
- `GET /v1/tiktok/video/comments` — Comments
- `GET /v1/tiktok/video/transcript` — Transcript
- `GET /v2/tiktok/video` — Video Info
- `GET /v3/tiktok/profile/videos` — Profile Videos

### facebook — 24
- `GET /v1/facebook/adLibrary/ad` — Ad Details
- `GET /v1/facebook/adLibrary/ad/transcript` — Ad Transcript
- `GET /v1/facebook/adLibrary/company/ads` — Company Ads
- `POST /v1/facebook/adLibrary/company/ads` — Company Ads
- `GET /v1/facebook/adLibrary/search/ads` — Search
- `POST /v1/facebook/adLibrary/search/ads` — Search
- `GET /v1/facebook/adLibrary/search/companies` — Search for Companies
- `GET /v1/facebook/event/details` — Event Details
- `GET /v1/facebook/events` — Events
- `GET /v1/facebook/events/search` — Search Events
- `GET /v1/facebook/group` — Facebook Group Info
- `GET /v1/facebook/group/posts` — Facebook Group Posts
- `GET /v1/facebook/marketplace/item` — Marketplace Item
- `GET /v1/facebook/marketplace/location/search` — Marketplace Location Search
- `GET /v1/facebook/marketplace/search` — Marketplace Search
- `GET /v1/facebook/post` — Post
- `GET /v1/facebook/post/comment/replies` — Comment Replies
- `GET /v1/facebook/post/comments` — Comments
- `GET /v1/facebook/post/transcript` — Transcript
- `GET /v1/facebook/profile` — Profile
- `GET /v1/facebook/profile/events` — Profile Events
- `GET /v1/facebook/profile/photos` — Profile Photos
- `GET /v1/facebook/profile/posts` — Profile Posts
- `GET /v1/facebook/profile/reels` — Profile Reels

### instagram — 19
- `GET /v1/instagram/audio/reels` — Get Reels By Audio Id
- `GET /v1/instagram/basic-profile` — Basic Profile
- `GET /v1/instagram/post` — Post/Reel Info
- `GET /v1/instagram/post/comment/replies` — Comment Replies
- `GET /v1/instagram/profile` — Profile
- `GET /v1/instagram/reels/trending` — Trending Reels
- `GET /v1/instagram/search` — Search Instagram
- `GET /v1/instagram/search/hashtag` — Search Hashtag Posts
- `GET /v1/instagram/search/popular` — Popular Search
- `GET /v1/instagram/search/profiles` — Search Instagram Profiles
- `GET /v1/instagram/user/embed` — Embed HTML
- `GET /v1/instagram/user/highlight/detail` — Highlights Details
- `GET /v1/instagram/user/highlights` — Story Highlights
- `GET /v1/instagram/user/reels` — Reels
- `GET /v1/instagram/user/tagged-posts` — User Tagged Posts
- `GET /v2/instagram/media/transcript` — Transcript
- `GET /v2/instagram/post/comments` — Comments
- `GET /v2/instagram/reels/search` — Search Reels
- `GET /v2/instagram/user/posts` — Posts

### youtube — 16
- `GET /v1/youtube/channel` — Channel Details
- `GET /v1/youtube/channel-videos` — Channel Videos
- `GET /v1/youtube/channel/community-posts` — Channel Community Posts
- `GET /v1/youtube/channel/lives` — Channel Lives
- `GET /v1/youtube/channel/playlists` — Channel Playlists
- `GET /v1/youtube/channel/shorts` — Channel Shorts
- `GET /v1/youtube/community-post` — Community Post Details
- `GET /v1/youtube/playlist` — Playlist
- `GET /v1/youtube/search` — Search
- `GET /v1/youtube/search/hashtag` — Search by Hashtag
- `GET /v1/youtube/shorts/trending` — Trending Shorts
- `GET /v1/youtube/video` — Video/Short Details
- `GET /v1/youtube/video/comment/replies` — Comment Replies
- `GET /v1/youtube/video/comments` — Comments
- `GET /v1/youtube/video/sponsors` — Video Sponsors
- `GET /v1/youtube/video/transcript` — Transcript

### github — 10
- `GET /v1/github/repository` — Repository
- `GET /v1/github/trending/developers` — Trending Developers
- `GET /v1/github/trending/repositories` — Trending Repositories
- `GET /v1/github/user` — User
- `GET /v1/github/user/activity` — Activity
- `GET /v1/github/user/contributions` — Contributions
- `GET /v1/github/user/followers` — Followers
- `GET /v1/github/user/following` — Following
- `GET /v1/github/user/pull-requests` — Pull Requests
- `GET /v1/github/user/repositories` — Repositories

### linkedin — 8
- `GET /v1/linkedin/ad` — Ad Details
- `GET /v1/linkedin/ads/search` — Search Ads
- `GET /v1/linkedin/company` — Company Page
- `GET /v1/linkedin/company/posts` — Company Posts
- `GET /v1/linkedin/post` — Post
- `GET /v1/linkedin/post/transcript` — Post Transcript
- `GET /v1/linkedin/profile` — Person's Profile
- `GET /v1/linkedin/search/posts` — Search Posts

### reddit — 7
- `GET /v1/reddit/post/comments` — Post Comments
- `POST /v1/reddit/post/comments` — Post Comments
- `GET /v1/reddit/post/transcript` — Post Transcript
- `GET /v1/reddit/search` — Search
- `GET /v1/reddit/subreddit` — Subreddit Posts
- `GET /v1/reddit/subreddit/details` — Subreddit Details
- `GET /v1/reddit/subreddit/search` — Subreddit Search

### twitter — 6
- `GET /v1/twitter/community` — Community
- `GET /v1/twitter/community/tweets` — Community Tweets
- `GET /v1/twitter/profile` — Profile
- `GET /v1/twitter/tweet` — Tweet Details
- `GET /v1/twitter/tweet/transcript` — Transcript
- `GET /v1/twitter/user-tweets` — User Tweets

### spotify — 6
- `GET /v1/spotify/album` — Album
- `GET /v1/spotify/artist` — Artist
- `GET /v1/spotify/podcast` — Podcast
- `GET /v1/spotify/podcast/episodes` — Podcast Episodes
- `GET /v1/spotify/search` — Search
- `GET /v1/spotify/track` — Track

### rumble — 5
- `GET /v1/rumble/channel/videos` — Channel Videos
- `GET /v1/rumble/search` — Search
- `GET /v1/rumble/video` — Video
- `GET /v1/rumble/video/comments` — Comments
- `GET /v1/rumble/video/transcript` — Transcript

### threads — 5
- `GET /v1/threads/post` — Post
- `GET /v1/threads/profile` — Profile
- `GET /v1/threads/search` — Search by Keyword
- `GET /v1/threads/search/users` — Search Users
- `GET /v1/threads/user/posts` — Posts

### google — 4
- `GET /v1/google/ad` — Ad Details
- `GET /v1/google/adLibrary/advertisers/search` — Advertiser Search
- `GET /v1/google/company/ads` — Company Ads
- `GET /v1/google/search` — Search

### pinterest — 4
- `GET /v1/pinterest/board` — Board
- `GET /v1/pinterest/pin` — Pin
- `GET /v1/pinterest/search` — Search
- `GET /v1/pinterest/user/boards` — User Boards

### twitch — 4
- `GET /v1/twitch/clip` — Clip
- `GET /v1/twitch/profile` — Profile
- `GET /v1/twitch/user/schedule` — User Schedule
- `GET /v1/twitch/user/videos` — User Videos

### apple-music — 4
- `GET /v1/apple-music/album` — Album
- `GET /v1/apple-music/artist` — Artist
- `GET /v1/apple-music/search` — Search
- `GET /v1/apple-music/track` — Track

### account — 4
- `GET /v1/account/credit-balance` — Get credit balance
- `GET /v1/account/get-api-usage` — Get request history
- `GET /v1/account/get-daily-usage-count` — Get daily usage
- `GET /v1/account/get-most-used-routes` — Get most used routes

### truthsocial — 3
- `GET /v1/truthsocial/post` — Post
- `GET /v1/truthsocial/profile` — Profile
- `GET /v1/truthsocial/user/posts` — User Posts

### bluesky — 3
- `GET /v1/bluesky/post` — Post
- `GET /v1/bluesky/profile` — Profile
- `GET /v1/bluesky/user/posts` — Posts

### soundcloud — 3
- `GET /v1/soundcloud/artist` — Artist
- `GET /v1/soundcloud/artist/tracks` — Artist Tracks
- `GET /v1/soundcloud/track` — Track

### kwai — 3
- `GET /v1/kwai/post` — Post
- `GET /v1/kwai/profile` — Profile
- `GET /v1/kwai/user/posts` — User Posts

### snapchat — 3
- `GET /v1/snapchat/profile` — User Profile
- `GET /v1/snapchat/spotlight` — Spotlight by Link
- `GET /v1/snapchat/spotlight/comments` — Spotlight Comments by Link

### kick — 1
- `GET /v1/kick/clip` — Clip

### linktree — 1
- `GET /v1/linktree` — Linktree page

### komi — 1
- `GET /v1/komi` — Komi page

### pillar — 1
- `GET /v1/pillar` — Pillar page

### linkbio — 1
- `GET /v1/linkbio` — Linkbio page

### amazon — 1
- `GET /v1/amazon/shop` — Amazon Shop page

### detect-age-gender — 1
- `GET /v1/detect-age-gender` — Get Age and Gender

### linkme — 1
- `GET /v1/linkme` — Profile